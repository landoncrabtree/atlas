# atlas-mock-servers requirements.txt
#
# Prefer `uv sync` (see pyproject.toml). This file is a fallback for
# environments that only have plain pip.
paramiko>=3.4,<4
pyftpdlib>=1.5.10,<3
wsgidav>=4.3,<5
cheroot>=10,<11
moto[server]>=5.0,<6
boto3>=1.34,<2
flask>=3,<4
