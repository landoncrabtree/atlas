//! Cross-platform shims for OS-native UI details that live outside Slint's
//! model — currently just [`titlebar_theme`] for tinting the native title
//! bar to match Atlas's active light/dark theme.

pub mod titlebar_theme;
